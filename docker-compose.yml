version: '3.8'

services:
  postgres:
    image: postgres
    container_name: smortsogest-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: user_db
      TZ: Asia/Kolkata
    ports:
      - "5432:5432"